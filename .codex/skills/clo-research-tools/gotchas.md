# Research Tools Gotchas

- Utilities are explicit commands, not hidden hooks.
- `dashboard` and `html-report` create local project artifacts, not the public Dittonomics website.
- `lint` is advisory unless strict mode is requested.
- `checkpoint` and `resume-context` use `.codex-state/`; quality reports remain research workflow outputs.
- `upgrade` changes infrastructure, not paper content.
